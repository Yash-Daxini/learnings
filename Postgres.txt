Postgres can also store unstructured data in JSON format. For that it has JSONB data type.

Is supports cron job.

Provides real time database like firebase or supabase using electric sql.

JWT can implemented by own using pgcrypto & pgjwt.

pg_mooncake converts postgres to time series database and makes it very fast.

PostgREST automatically converts database into rest apis 
